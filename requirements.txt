Django>=5.0,<6.0
Pillow>=10.0            # image handling (company logo upload)
openpyxl>=3.1          # accountant Excel (.xlsx) exports
xhtml2pdf>=0.2.16      # PDF generation for sales documents (pure-Python)

# Production / deployment (optional for local dev with SQLite)
gunicorn>=21.2          # WSGI server
whitenoise>=6.6         # static file serving
psycopg2-binary>=2.9    # PostgreSQL driver
dj-database-url>=2.1    # DATABASE_URL parsing
